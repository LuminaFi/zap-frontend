import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "placehold.co",
      "cryptologos.cc",
      "zap-service-jkce.onrender.com",
      "s2.coinmarketcap.com",
    ],
  },
};

export default nextConfig;
